spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invoicing_db?useSSL=false&serverTimezone=UTC
    username: freelancer
    password: freelancer123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080